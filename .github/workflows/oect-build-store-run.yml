name: OECT Build Store Run

on:
  workflow_dispatch:
    inputs:
      release_tag:
        description: New immutable release tag to create, or an existing tag that already points to this commit
        required: true
        default: oect-store-run-20260707-r1
      store_base_url:
        description: iStore package repository base URL
        required: true
        default: https://repo.istoreos.com/repo/all/store/

permissions:
  contents: write

env:
  RELEASE_TAG: ${{ inputs.release_tag }}
  STORE_BASE_URL: ${{ inputs.store_base_url }}

jobs:
  build-store-run:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install tools
        run: |
          sudo apt-get update
          sudo apt-get install -y makeself curl

      - name: Download whitelisted ipks
        run: |
          chmod +x oect/*.sh
          ./oect/download-store-ipks.sh "$STORE_BASE_URL" build/store build/source-manifest.tsv
          cp oect/install.sh build/store/install.sh
          chmod +x build/store/install.sh

      - name: Security scan payload
        run: |
          ./oect/security-scan.sh build/store oect/security-denylist.txt

      - name: Package run artifact
        run: |
          set -eu
          mkdir -p dist
          version="$(awk -F_ '/^luci-app-store_/ {print $2; exit}' < <(cd build/store && ls luci-app-store_*.ipk))"
          if [ -z "$version" ]; then
            echo "ERROR: cannot detect luci-app-store version" >&2
            exit 30
          fi
          makeself build/store "dist/oect-luci-app-store-${version}_all.run" "OECT iStore installer ${version}" ./install.sh
          cp build/source-manifest.tsv dist/source-manifest.tsv
          cp oect/README.md dist/SAFETY.md

      - name: Ensure immutable release tag
        run: |
          chmod +x oect/ensure-release-tag.sh
          ./oect/ensure-release-tag.sh "$RELEASE_TAG"

      - name: Create release metadata
        run: |
          set -eu
          ./oect/production-manifest.sh "$RELEASE_TAG" store-run > dist/manifest.json
          (cd dist && sha256sum * > SHA256SUMS)

      - name: Publish Store run release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          name: OECT Store run ${{ env.RELEASE_TAG }}
          body_path: dist/SAFETY.md
          prerelease: true
          make_latest: false
          overwrite_files: true
          files: |
            dist/*
